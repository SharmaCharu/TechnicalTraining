var person={
    name:'Charu',
    age:20,
    isMarried:false,
};
var age1=3;
var category1=getCategory(age1);
console.log(Category);

function Category(age)
{
    if(age >=0 && age<5)
    Category=baby;
}